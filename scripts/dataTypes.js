/**
 *  This script is a test from class
 *
 */

let laborDay = new Date("September 1, 2019"); // object
    let age; // undefined
    document.write(typeof(laborDay) + "<br>");
    document.write(typeof(age) + "<br>");
    document.write(typeof("Mel's Diner");
    document.write(typeof(age));